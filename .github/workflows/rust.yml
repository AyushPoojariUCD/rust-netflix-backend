name: Rust

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    runs-on: ubuntu-latest

    env:
      TMDB_API_KEY: ${{ secrets.TMDB_API_KEY }}

    steps:
      - uses: actions/checkout@v4

      - name: Build
        run: cargo build --verbose

      - name: Run tests
        run: cargo test --verbose

      - name: Start server
        run: |
          cargo run &
          sleep 5

      - name: Test API endpoints
        run: |
          curl --fail http://localhost:8080/api/trending
          curl --fail "http://localhost:8080/api/search?query=matrix"
          curl --fail http://localhost:8080/api/movie/603/videos
